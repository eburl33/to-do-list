<!-- To begin project properly:


```bash
docker-compose up -d
cd backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python -m uvicorn main:app --reload
create new terminal
cd frontend
npm run dev
backend url: http://127.0.0.1:8000/docs#/
frontend url: http://localhost:5173/
-->
<!--

Functionality:

Create a new list or choose from an existing list
Add tasks to To Do List
The item will populate the list field
Move them up and down in priority, complete them, or delete them -->
